/** The GitHub user whose activity fills the museum. */
export const GITHUB_USER = 'sliekens';

/** The repository that hosts this site (gets a special exhibit plaque). */
export const SITE_REPO = 'sliekens.github.io';

/** Build-time generated data snapshot, served as a static asset. */
export const DATA_URL = '/data/github.json';

export const FONT_REGULAR = '/fonts/JetBrainsMono-Regular.ttf';
export const FONT_BOLD = '/fonts/JetBrainsMono-Bold.ttf';
